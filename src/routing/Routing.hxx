#pragma once

namespace hermes
{

  /**
   * @brief Routing decides where to route the orders into to get best pricing
   * or returns based on some strategies
   */
  template <typename Derived>
  class Routing {
  };

};